{
  "name": "basicwebrtc",
  "version": "1.0.0",
  "description": "Basic WebRTC application without signalling i.e. local and peer on same window",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Prashant Sihag",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1"
  }
}
